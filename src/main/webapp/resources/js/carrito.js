jQuery(function () {
/*

    $(document).on('click', '.addLibroCarrito', function () {
        showLoader();
        let form = $(this).closest('form');

        $.ajax({
            url: form.attr('action'),
            type: "POST",
            data: form.serialize(),
            success: function (fragment) {
                console.log(USER);

                //$('#cesta-simplificada').html(fragment);
                //$('#cesta-simplificada-button').click();
            }, complete: function () {
                hideLoader();
            }
        });
    });*/
});
